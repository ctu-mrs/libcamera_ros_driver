# Set the user and group ID for the development container.
# These should match your host user's UID/GID to avoid file permission issues.
# You can find your UID with `id -u` and GID with `id -g`.
USER_UID=1000
USER_GID=1000
